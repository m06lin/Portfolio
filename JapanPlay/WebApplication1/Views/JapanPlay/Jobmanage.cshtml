@*@(Html.Kendo().TabStrip()
          .Name("tabstrip")
          .Animation(animation =>
              animation.Open(effect =>
                  effect.Fade(FadeDirection.In)))
          .Items(tabstrip =>
          {
              tabstrip.Add().Text("開銷花費")
                  .Selected(true)
                  .LoadContentFrom(Url.Content("~/AllCost/Index"));


              tabstrip.Add().Text("景點評價")
                .LoadContentFrom(Url.Content("~/AllScene/Index"));

          })
    )*@
<div id="example">
    <div class="wrapper">
        <div id="tabstrip">
            <ul>
                <li class="k-state-active">職位管理</li>
                <li>帳號權限設定</li>

            </ul>
        </div>
    </div>
</div>

<script>
    $(document).ready(function () {
        var ts = $("#tabstrip").kendoTabStrip({
            animation: { open: { effects: "fadeIn" } },
            select: onSelect,
            contentUrls: [
                        '../Job/Index',
                        '../Job/AccountPower'

                    ]
        }).data('kendoTabStrip');
    });
    function onSelect(e) {
        //clear the current content of the tab before it's displayed to force the tabStrip to load it
        $(e.contentElement).html("");
    }
</script>